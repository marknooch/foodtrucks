name: createGeoJson
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *'
  
jobs:
  createGeoJson-job:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v2
      - name: produce geojson
        id: produce-geojson
        shell: pwsh
        run: ./createGeoJson.ps1
        working-directory: ./.github/workflows
      - name: set CURRENT_BRANCH and do git prep steps
        if: always()
        run: |
          echo "CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)" >> $GITHUB_ENV
          git config --global user.name 'atlantis-green'
          git config --global user.email 'atlantis-green@users.noreply.github.com'
          git add .
          git commit -m "Update the data"
          git fetch
      - name: commit files to data branch   
        run: git switch data
      - name: commit files to the error branch if produce-geojson failed
        if:  ${{ steps.produce-geojson.conclusion == 'failure'}}
        run: git switch error
      - name: finish committing the stuff
        if: always()
        run:
          cd ./data
          rm * -r
          git cherry-pick ${{ env.CURRENT_BRANCH }} --strategy-option=theirs
          git add . 
          git commit -m "Update the data"
          git push -u origin HEAD
      - name: download the geojson schema
        run: curl https://geojson.org/schema/GeoJSON.json --output GeoJSON.json
      - name: confirm we produced valid GeoJSON
        uses: orrosenblatt/validate-json-action@a7082767d95a588f79b1d742a835b11bbfd4ea32 # 0.1.0 -- I use commit hashes to reduce supply chain attacks --- https://michaelheap.com/ensure-github-actions-pinned-sha/
        env:
          INPUT_SCHEMA: GeoJSON.json
          INPUT_JSONS: /data/openFoodTrucks.json